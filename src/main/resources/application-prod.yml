spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mariadb://211.110.44.79:3306/ezylab_prod
    username: itadmin #사용자
    password: gthkds!83! #비밀번호
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

filepath:
  image: /data/uploads/ezylab/
  attachment: /data/uploads/ezylab/attachments/
